# Firebase Client Configuration (publicly available)
# These are safe to expose on the client-side and are prefixed with NEXT_PUBLIC_
NEXT_PUBLIC_FIREBASE_API_KEY="your_public_api_key_here"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your_project_id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your_project_id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your_project_id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your_sender_id"
NEXT_PUBLIC_FIREBASE_APP_ID="your_app_id"

# Firebase Admin SDK Configuration (server-side only)
# These must be kept secret and should only be used in server-side code (API routes).
# Do NOT prefix these with NEXT_PUBLIC_
FIREBASE_PROJECT_ID="your_project_id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-...@your_project_id.iam.gserviceaccount.com"
# The private key needs to be a single line string. You can get this from your Firebase project's service account JSON file.
# It's often easier to store the whole JSON config as a single base64 encoded string.
# Example for private key: "private_key_from_json_file"
FIREBASE_PRIVATE_KEY="your_private_key_here"